import { SyncLoader } from "react-spinners";

const Loader = () => {
    return(
        <div className="loader">
            <SyncLoader color="#000" />
        </div>
    )
}

export default Loader;